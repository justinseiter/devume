<section>
  <div class="row">
    <h2 class="section-head">Me</h2>
    <p>{{basics.summary}}</p>
    <div class="picture-wrap">
      <img alt="{{basics.name}}" class="picture" src="{{basics.picture}}" />
      <caption>Thanks!</caption>
    </div>
  </div>
</section>