<section class="education-wrap">
  <div class="education">
    <h2 class="section-head">Education</h2>
    <table>
      <tbody>
        {{#each education}}
        <tr>
          <td>{{this.institution}}</td>
          <td>{{this.studyType}} {{this.area}}</td>
          <td class="u-text-right">
            {{#if this.startDate}}{{this.startDate}}-{{/if}}{{this.endDate}}
          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</section>